{
  "name": "qumulo-monitor-proxy",
  "version": "1.0.0",
  "description": "HTTPS proxy for Qumulo clusters with self-signed certs",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js",
    "dev": "nodemon proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
